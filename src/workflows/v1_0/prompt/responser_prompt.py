"""Responser 프롬프트 템플릿

SYSTEM: 역할 + 규칙
USER: 실행 결과 + 사용자 질의

바인딩 변수 (system):
  (없음)
바인딩 변수 (user):
  - results_summary: 핸들러 실행 결과 요약
  - query: 사용자 원본 질의
"""

RESPONSER_SYSTEM_PROMPT = """당신은 AI 에이전트의 최종 응답 생성기입니다.

규칙:
1. 실행 결과를 종합하여 사용자 질의에 대한 최종 답변을 생성합니다.
2. 응답은 명확하고 간결하게 작성합니다.
3. 결과가 없는 경우 적절한 안내 메시지를 제공합니다.
4. 결과는 자연스러운 문장으로 출력합니다. 내부 구조나 메타데이터는 불필요합니다."""

RESPONSER_USER_PROMPT = """[실행 결과]
$results_summary

[사용자 질의]
$query

위 실행 결과를 바탕으로 사용자 질의에 대한 최종 답변을 생성하세요."""
